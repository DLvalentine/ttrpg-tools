# devLoot.yaml
# Test data for "nested tables" feature
# TODO playing around with possible changes to nested/plural/quantity

info:
  name: (Dev) Loot
  description: Test table containing loot rewards
  diceString: d1 # only one possible value, for testing
  exposed: [description, contents, quantity]
  dependencies: [devGems]
values:
  - roll: 1
    # TODO document change: toplevel flags are no more
    # TODO test rich composition: sack of loot: [1, 4] rolled gems, 2 rolled junk, 1 rolled weapon
    # TODO test rich composition: ^ but two sacks, then ^ but [1, 4] sacks
    # TODO test rich composition and document: contents is an array
    contents: $n|devGems:[1,4] # TODO document change: string tokens
    quantity: 1 # TODO document change: toplevel quantity behaves the same, has no impact on field nested rolls
    description: A sack of gems